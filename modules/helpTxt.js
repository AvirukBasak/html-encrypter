'use strict';

exports.helpTxt = ('USAGE:\n'
                 + '    index 64img | base64img [url] > file\n'
                 + '        Download an image and convert to b64\n'
                 + '    index ehtml | encryptHtml [filePath] [passwd] > file\n'
                 + '        Encrypt an HTML document');
